{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "deploy": "npm run docs:build && cd docs/.vitepress/dist && git init && git remote add origin https://github.com/ADereusme/NoteBook.git && git checkout -b gh-pages && git add . && git commit -m 'deploy' && git push -f origin gh-pages",
    "docs:preview": "vitepress preview docs"
  }
}
